import { Box } from "@mui/material";
import React from "react";
import Home from "../pages/home";

type Props = {};

const Ui = (props: Props) => {
  return (
    <Box>
      <Home />
    </Box>
  );
};

export default Ui;
