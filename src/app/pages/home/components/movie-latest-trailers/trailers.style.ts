import styled from "styled-components";

const TrailerStyle = styled.div`
  .movie__trailer-title-wrapper {
    display: flex;
    align-items: center;
    gap: 24px;
  }
`;

export default TrailerStyle;
